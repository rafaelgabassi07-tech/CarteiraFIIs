
/**
 * SERVIÇO DE IA DESATIVADO
 * O app agora opera exclusivamente com dados determinísticos (Scraper/API).
 */

export const generateInflationAnalysis = async (
  inflationRate: number,
  portfolioYield12m: number,
  realYield: number,
  coverageRatio: number
): Promise<string> => {
  return "Análise de IA desativada. Foco em dados reais.";
};

export const predictDividendSchedule = async (tickers: string[]): Promise<any[]> => {
  return [];
};
